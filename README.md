# 🎬 Shorts Maker API

FastAPI приложение для автоматической нарезки видео на шортсы с мобильной адаптацией.

## ✨ Возможности

- **Автоматическая нарезка** видео по сценам и паузам
- **Мобильная адаптация** в формат 9:16 с размытым фоном
- **Субтитры** с помощью Whisper AI (опционально)
- **Асинхронная обработка** с отслеживанием прогресса
- **REST API** для интеграции с другими сервисами
- **Поддержка множества форматов** (MP4, AVI, MKV, MOV, WMV, FLV, WebM, M4V)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` (уже создан скриптом инициализации):

```env
UPLOAD_PATH=./storage/input
OUTPUT_PATH=./storage/output
TEMP_PATH=./storage/temp
MAX_FILE_SIZE=500000000
ALLOWED_EXTENSIONS=.mp4,.avi,.mkv,.mov,.wmv,.flv,.webm,.m4v
MIN_DURATION_DEFAULT=60
MAX_DURATION_DEFAULT=180
ENABLE_SUBTITLES_DEFAULT=false
MOBILE_SCALE_DEFAULT=1.2
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=true
WHISPER_MODEL=base
```

### 3. Запуск сервера

```bash
uvicorn app.main:app --reload
```

Или через Python:

```bash
python -m app.main
```

### 4. Проверка работы

- **API документация**: http://localhost:8000/docs
- **Альтернативная документация**: http://localhost:8000/redoc
- **Проверка здоровья**: http://localhost:8000/api/v1/health

## 📋 API Endpoints

### Основные эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/api/v1/process` | Загрузка и обработка видео |
| `GET` | `/api/v1/status/{task_id}` | Получение статуса задачи |
| `GET` | `/api/v1/download/{file_path}` | Скачивание готового файла |
| `GET` | `/api/v1/algorithms` | Список доступных алгоритмов |
| `GET` | `/api/v1/health` | Проверка здоровья сервиса |
| `DELETE` | `/api/v1/cleanup/{task_id}` | Очистка файлов задачи |

### Пример использования

#### 1. Загрузка видео

```bash
curl -X POST "http://localhost:8000/api/v1/process" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@video.mp4" \
  -F "min_duration=60" \
  -F "max_duration=180" \
  -F "enable_subtitles=false" \
  -F "mobile_scale_factor=1.2"
```

Ответ:
```json
{
  "task_id": "task_1699123456_abc12345",
  "status": "queued",
  "message": "Задача добавлена в очередь на обработку"
}
```

#### 2. Проверка статуса

```bash
curl "http://localhost:8000/api/v1/status/task_1699123456_abc12345"
```

Ответ:
```json
{
  "task_id": "task_1699123456_abc12345",
  "status": "completed",
  "progress": 100,
  "message": "Обработка завершена успешно. Создано сегментов: 3",
  "result_files": [
    "/api/v1/download/task_1699123456_abc12345/video_part_01_120s.mp4",
    "/api/v1/download/task_1699123456_abc12345/video_part_02_95s.mp4",
    "/api/v1/download/task_1699123456_abc12345/video_part_03_140s.mp4"
  ],
  "created_at": "2024-01-01T10:00:00Z",
  "updated_at": "2024-01-01T10:05:30Z"
}
```

#### 3. Скачивание файлов

```bash
curl -O "http://localhost:8000/api/v1/download/task_1699123456_abc12345/video_part_01_120s.mp4"
```

## 🏗️ Архитектура проекта

```
app/
├── main.py                 # Главное FastAPI приложение
├── api/                    # API роуты
│   └── routes/
│       └── processing.py   # Эндпоинты обработки видео
├── core/                   # Основные компоненты
│   ├── content_analyzer.py # Анализ видео (сцены, паузы)
│   ├── segment_extractor.py# Извлечение сегментов
│   ├── mobile_converter.py # Мобильная конвертация
│   ├── subtitle_engine.py  # Генерация субтитров
│   └── video_processor.py  # Координатор обработки
├── algorithms/             # Алгоритмы нарезки
│   └── multi_shorts.py     # Алгоритм множественных шортсов
├── models/                 # Pydantic модели
│   └── schemas.py          # API схемы
├── services/               # Бизнес-логика
│   └── video_service.py    # Сервис управления задачами
└── config/                 # Настройки
    └── settings.py         # Конфигурация приложения
```

## ⚙️ Алгоритмы обработки

### Multi Shorts (`multi_shorts`)

Нарезает одно видео на несколько коротких сегментов:

1. **Анализ видео** - поиск смен сцен и пауз в аудио
2. **Определение точек разреза** - логические моменты для нарезки
3. **Извлечение сегментов** - создание отдельных файлов
4. **Мобильная конвертация** - формат 9:16 с размытым фоном
5. **Субтитры** - автоматическая генерация (опционально)

**Параметры:**
- `min_duration` - минимальная длительность сегмента (30-300 сек)
- `max_duration` - максимальная длительность сегмента (60-600 сек)  
- `enable_subtitles` - включить генерацию субтитров
- `mobile_scale_factor` - коэффициент увеличения основного видео (1.0-2.0)

## 📦 Структура хранения

```
storage/
├── input/          # Загруженные файлы
├── output/         # Готовые результаты
│   └── {task_id}/  # Папка для каждой задачи
└── temp/           # Временные файлы
    └── tasks/      # Статусы задач (JSON)
```

## 🔧 Требования системы

- **Python** 3.8+
- **FFmpeg** (полная версия с поддержкой субтитров)
- **Свободное место** - зависит от размера обрабатываемых видео

### Установка FFmpeg

**Windows:**
1. Скачайте с https://ffmpeg.org/download.html
2. Добавьте в PATH

**Linux:**
```bash
sudo apt update
sudo apt install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

## 🚀 Развертывание

### Docker (рекомендуется)

```dockerfile
FROM python:3.11-slim

# Установка FFmpeg
RUN apt-get update && apt-get install -y ffmpeg

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Systemd Service

```ini
[Unit]
Description=Shorts Maker API
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/path/to/shorts-maker-api
Environment=PATH=/path/to/venv/bin
ExecStart=/path/to/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
```

## 🔒 Безопасность

- **Валидация файлов** - проверка размера и формата
- **Изоляция путей** - предотвращение path traversal
- **Очистка временных файлов** - автоматическое удаление
- **CORS настройки** - конфигурируемые домены

## 📈 Мониторинг

- **Health check**: `/api/v1/health`
- **Логирование** - вывод в консоль
- **Статусы задач** - файловое хранение

## 🤝 Интеграция с Telegram

API готов для интеграции с Telegram ботами:

1. **Загрузка файлов** через file_id или URL
2. **Асинхронная обработка** с уведомлениями
3. **Создание ZIP архивов** с готовыми шортсами
4. **Статус-бар** прогресса выполнения

## 📝 Лицензия

MIT License - см. файл LICENSE для деталей.

## 🐛 Сообщить об ошибке

Создайте issue в репозитории с описанием проблемы и шагами воспроизведения.